import { checkTitleLength } from "../js/titleChecker"

describe("Testing the checkTitleLength functionality", () => {
    test("Testing the checkTitleLength() function is defined", () => {
        expect(checkTitleLength).toBeDefined();
    })
})