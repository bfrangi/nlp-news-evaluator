import { checkForName } from "../js/nameChecker"

describe("Testing the checkForName functionality", () => {
    test("Testing the checkForName() function is defined", () => {
        expect(checkForName).toBeDefined();
    })
})